This my current vim configuration.

I use vim mainly for coding (C++, python, haskell) and LaTeX. It is configured
with this tasks in mind.

vimrc and gvimrc should be linked into $HOME/{.vimrc,.gvimrc}
